apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "erp.fullname" . }}-backend
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "erp.fullname" . }}-backend
  template:
    metadata:
      labels:
        app: {{ include "erp.fullname" . }}-backend
    spec:
      containers:
        - name: backend
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          ports:
            - containerPort: 3000
